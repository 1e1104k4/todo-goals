<div id="<%= dom_id goal %>">
  <li class="py-2 px-4 border-b">
    <div><%= goal.title %></div>
    <div><%= goal.description %></div>

    <%= link_to "Edit this goal", edit_goal_path(goal), class: "mt-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
    <div class="inline-block ml-2">
      <%= button_to "Destroy this goal", goal, method: :delete, class: "mt-2 rounded-md px-3.5 py-2.5 text-white bg-red-600 hover:bg-red-500 font-medium" %>
    </div>

  </li>
</div>
